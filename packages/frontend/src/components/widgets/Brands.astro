---
import type { Brands as Props } from '#/types';

import { Image } from 'astro:assets';
import Headline from '#/components/ui/Headline.astro';
import WidgetWrapper from '#/components/ui/WidgetWrapper.astro';
const {
  title = '',
  subtitle = '',
  tagline = '',
  icons = [],
  images = [],
  description = false,
  id,
  isDark = false,
  classes = {},
  bg
} = Astro.props;
---

<WidgetWrapper id={id} isDark={isDark} containerClass={`max-w-6xl mx-auto ${classes?.container ?? ''}`} bg={bg}>
  <Headline title={title} subtitle={subtitle} tagline={tagline} />

  <div class="flex flex-wrap justify-center gap-x-6 sm:gap-x-12 lg:gap-x-24">
    {icons && icons.map(Icon => <Icon class="py-3 lg:py-5 w-12 h-auto mx-auto sm:mx-0 text-gray-500" />)}
    {
      images
      && images.map(
        image => (
          <div class="flex justify-center col-span-1 my-2 lg:my-4 py-1 px-3 rounded-md dark:bg-gray-200">
            <div class="flex lg:flex-nowrap flex-wrap justify-center items-center">
              <a class="min-w-sm mb-10 ml-10" href={image.href} target="_blank">
                <div class="flex flex-col items-center justify-center">
                  {/* @ts-expect-error - Track https://github.com/withastro/astro/issues/10780 */}
                  <Image class="max-h-16" height={64} {...image} href={undefined} />
                  {image.alt && <p class="text-lg font-semibold">{image.alt}</p>}
                </div>
              </a>
              {description && image.description && 
                <blockquote class="text-muted text-lg font-oblique font-serif" set:html={image.description} />
              }
            </div>
          </div>
        )
      )
    }
  </div>
</WidgetWrapper>
